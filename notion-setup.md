# 노션(Notion) 연동 및 설정 가이드

구글 뉴스를 노션 데이터베이스에 자동으로 저장하기 위해 아래 단계를 진행해 주세요.

---

## 1. 노션 API 통합(Integration) 만들기

1. [노션 개발자 포털 (Notion My Integrations)](https://www.notion.so/my-integrations)에 접속하고 로그인합니다.
2. **`+ 새 통합` (New Integration)** 버튼을 클릭합니다.
3. 다음과 같이 작성합니다:
   - **이름:** `n8n News Collector` (또는 자유롭게 설정)
   - **워크스페이스:** 해당 노션 워크스페이스 선택
   - **콘텐츠 권한:** `읽기`, `업데이트`, `삽입` 모두 허용
4. 설정을 저장하면 생성되는 **프라이빗 API 통합 토큰 (Internal Integration Token)**을 복사하여 안전한 곳에 보관해 둡니다. (이 토큰을 n8n에 입력하게 됩니다.)

---

## 2. 뉴스 저장용 노션 데이터베이스(표) 만들기

1. 노션에서 새 페이지를 생성한 후 **`데이터베이스 - 표` (Table Database)**를 생성합니다.
2. 데이터베이스 속성(컬럼)을 다음과 같이 정확하게 구성해 주세요 (이름 대소문자 주의):

| 속성 이름 | 속성 유형 (Type) | 역할 |
| :--- | :--- | :--- |
| **Title** | `제목 (Title)` | 뉴스 제목 저장 |
| **URL** | `URL` | 뉴스 링크 저장 |
| **Published Date** | `날짜 (Date)` | 뉴스 게시일 저장 |

3. (선택사항) 필요하지 않은 기본 속성(예: '태그')은 삭제하셔도 좋습니다.

---

## 3. 데이터베이스에 API 권한 부여하기 (중요)

노션 페이지 보안상 API 통합이 해당 페이지에 접근할 수 있도록 권한을 허용해야 합니다.

1. 방금 만든 뉴스 데이터베이스 페이지 우측 상단의 **`... (점 세개)`** 버튼을 누릅니다.
2. 맨 아래쪽의 **`연결 추가 (Add Connections)`**를 클릭합니다.
3. 앞서 생성한 **`n8n News Collector`**를 검색하여 추가합니다.

---

## 4. 데이터베이스 ID 확인하기

n8n 설정 시 데이터베이스 ID가 필요합니다.

1. 데이터베이스 페이지의 공유 URL을 복사합니다.
   - 예: `https://www.notion.so/myworkspace/a8bef932b1394a123456789abcdef123?v=...`
2. URL 경로 중에서 `myworkspace/` 바로 뒤부터 `?v=` 직전까지의 **32자리 난수**가 데이터베이스 ID입니다.
   - 위의 예시의 경우: `a8bef932b1394a123456789abcdef123`

---

## 5. n8n에서 연동하기

1. n8n 관리자 페이지(`http://192.168.0.11:5678`)에 접속합니다.
2. 좌측 메뉴의 **Credentials** -> **Add Credential** -> **Notion API**를 선택합니다.
3. 복사한 **Internal Integration Token**을 입력하고 저장합니다.
4. 제가 주입해 둔 **`Google News to Notion Daily`** 워크플로우를 열고, **Notion** 노드를 더블클릭합니다.
5. 복사한 **Database ID**를 입력한 후 워크플로우를 활성화(Active)합니다.
